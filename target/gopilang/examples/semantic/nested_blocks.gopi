// nested_blocks.gopi
// Expected: 0 diagnostics.
// A bare nested block executes unconditionally (unlike an if/loop branch),
// so an assignment made inside it is visible to code after the block ends.

none main() {
    num x;
    {
        x = 5;
    }
    show(x);
}

// nested_control_flow.gopi - nested if inside loop

none main() {
    num x = 10;
    loop (x > 0) {
        if (x == 5) {
            show(x);
            x = x - 1;
        } else {
            x = x - 1;
        }
    }
}

// nested_control_flow.gopi - nested if inside while

void main() {
    int x = 10;
    while (x > 0) {
        if (x == 5) {
            print(x);
            x = x - 1;
        } else {
            x = x - 1;
        }
    }
}
